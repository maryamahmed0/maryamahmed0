name: Generate snake game from GitHub contribution grid

on:
  schedule:
    - cron: "0 */12 * * *"   # كل 12 ساعة
  workflow_dispatch:

permissions:
  contents: write            # لازم عشان يقدر يـpush

jobs:
  generate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: write        # تأكيد تاني على مستوى الـjob

    steps:
      # (اختياري) Checkout — مش ضروري، بس بيفيد لو فيه قواعد حماية/إعدادات repo
      - name: Checkout
        uses: actions/checkout@v4

      - name: Generate SVGs
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: maryamahmed0
          outputs: |
            dist/github-contribution-grid-snake.svg
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Check generated files (diagnostic)
        run: |
          echo "Root:"
          ls -la
          echo "---- dist ----"
          ls -la dist || true
          test -s dist/github-contribution-grid-snake.svg
          test -s dist/github-contribution-grid-snake-dark.svg

      - name: Publish to output branch
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: dist
          publish_branch: output
          force_orphan: false
          user_name: "github-actions[bot]"
          user_email: "github-actions[bot]@users.noreply.github.com"
